/*
    A DELIBERATELY BROKEN pack set. packlint must fail on it and name every
    fault -- a linter that only ever says "fine" has never been tested.
*/
scenes
{
	numScenes 2
	pack 0	data/levels/wrong/pack.cfg
	pack 0	data/levels/wrong/pack.cfg		# the same id twice
	pack 99	data/levels/wrong/pack.cfg		# out of range
}
